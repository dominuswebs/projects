// running node nodemon.js requires stopping the application and restarting
// running nodemon nodemon.js enables Hot reload. We can see the effect of code changes in a running application without needing to restart it

setInterval(()=>{console.log("1 second.")}, 1000);